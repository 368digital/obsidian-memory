{
  "name": "obsidian-claude-memory",
  "version": "0.3.0",
  "description": "Obsidian plugin for Claude Code — full project memory with file tracking, wikilinks, sidebar log, dashboard, timeline",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.8.0"
  }
}
